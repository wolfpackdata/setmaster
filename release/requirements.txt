# SetMaster 3 - locked RUNTIME dependencies for release payloads (#179).
#
# Runtime only: no pytest/httpx or other dev-only packages. Every pin is the
# version installed in backend/.venv, i.e. the exact set the test suites and the
# golden-master pipeline comparison were run against - a release must ship what
# was tested, not a fresh resolve.
#
# Regenerate: install backend/pyproject.toml's [project] dependencies into a
# throwaway venv to learn the runtime closure, then pin each of those names to
# its backend/.venv version. Bump deliberately, re-run the suites, re-lock.
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.1
click==8.4.2
colorama==0.4.6
et_xmlfile==2.0.0
fastapi==0.139.0
h11==0.16.0
idna==3.18
numpy==2.5.0
openpyxl==3.1.5
pandas==2.3.3
pydantic==2.13.4
pydantic_core==2.46.4
python-dateutil==2.9.0.post0
python-multipart==0.0.32
pytz==2026.2
regex==2026.6.28
six==1.17.0
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
tzdata==2026.2
uvicorn==0.50.2
